table.table.table-condensed
  thead
  tr
    th Name
    th Status
    th Options

  tbody
    - rooms.each do |room|
      tr
        td = link_to room.name, room
        td = render 'rooms/room_status_toggle', room: room

        td = link_to 'Edit', edit_room_path(room)